{% if isAuth  %}
    Добро пожаловать {{ username }} <a href="/auth/logout/">[Выход]</a>
{% else  %}
    <form action="/auth/login/" method="POST">
        <input type="text" name="login" value="admin" placeholder="Логин">
        <input type="text" name="password" value="123" placeholder="Пароль">
        <input type="submit" name="submit" value="Войти">
    </form>
{% endif %}
<br>
<a href="/">Главная</a>
<a href="/product/catalog">Каталог</a>
<a href="/basket">Корзина <span>({{ count }})</span></a>
<a href="/feedback">Отзывы</a>
<br>
